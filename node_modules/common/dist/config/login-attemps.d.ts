export declare abstract class LoginAttempsConfig {
    static loginAttemps: {
        hours: number;
        attemps: number;
    };
    static mpinResetDate: number;
    static loginAttemptViaAD: {
        attempts: number;
    };
}
